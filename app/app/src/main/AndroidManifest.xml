<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> <!-- 안드로이드 13 이상 (티라미수) 대응 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Travelapp"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".ui.login.SignupActivity"
            android:exported="false" />
        <activity
            android:name=".ui.mypage.UserEditActivity"
            android:exported="false" />
        <activity
            android:name=".ui.chat.ChatDetailActivity"
            android:exported="false" />
        <activity
            android:name=".ui.trip.TripMapActivity"
            android:exported="false" />
        <activity
            android:name=".ui.trip.TripCreateActivity"
            android:exported="false" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${googleApiKey}" />

        <activity
            android:name=".ui.trip.TripDetailActivity"
            android:exported="false" />
        <activity
            android:name=".ui.board.PostWriteActivity"
            android:exported="false" />
        <activity
            android:name=".ui.board.PostDetailActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>