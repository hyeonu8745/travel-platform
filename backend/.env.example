####################################
# .env.example
# 이 파일을 복사하여 .env 파일을 만들고, 실제 비밀 값을 채워 사용하세요.
# 이 파일 자체에는 실제 비밀 값을 넣지 않습니다.
####################################

# 1. 서버 설정
PORT=3000

# 2. 데이터베이스 설정 (MySQL)
DB_HOST=localhost
DB_DATABASE=travel_db
DB_USERNAME=root
DB_PASSWORD=YOUR_DB_PASSWORD_HERE

# 3. 인증 (JWT) 비밀 키
# JWT 토큰 서명 및 검증에 사용될 비밀 키입니다. 보안을 위해 길고 무작위적인 문자열을 사용하세요.
JWT_SECRET_KEY=YOUR_LONG_AND_RANDOM_JWT_SECRET

# 4. 외부 서비스 API 키
# Google 지도 및 장소 검색, 지도 표시에 사용됩니다.
GOOGLE_MAPS_API_KEY=YOUR_GOOGLE_MAPS_API_KEY_HERE